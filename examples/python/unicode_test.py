greeting = "Hello"
café = "latte"  # identifier with multi-byte char, é = 2 bytes
x = "données"  # multi-byte in string, then identifier on next lines
résumé = café  # both identifiers have multi-byte chars
print(café, résumé)
